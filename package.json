{
  "name": "pnpm-hooks",
  "version": "0.0.0",
  "private": true,
  "description": "Apply explicit workspace policy to rotating pnpm registry shard URLs",
  "type": "module",
  "scripts": {
    "lint": "node --check .pnpmfile.cjs && node --check apply-pnpm-tarball-policy.mjs && node --check scripts/docker-test.mjs && node --check test/docker-compat.mjs && node --check test/solution.test.mjs",
    "test": "node --test test/solution.test.mjs",
    "test:docker": "node scripts/docker-test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
