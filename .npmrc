# Canonical workspace registry. Policy, integrity limitations, and repair:
# SOURCES.md#lockfile-integrity.
#
# The feed is public and requires no token. This global install-time setting
# does not redirect publishes; package publishConfig and scoped registries win.
registry=https://packagefeedproxy.microsoft.io/npm/
